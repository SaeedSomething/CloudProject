apiVersion: v1
kind: Service
metadata:
    name: manage
    namespace: manage
spec:
    type: NodePort
    selector:
        app: manage
    ports:
        - port: 8080
          targetPort: 8080
          nodePort: 30081
